# A Basic CLI Tool that:

- Accepts a folder path as a command-line argument.
- Lists all .js files in the folder recursively
- counts lines of code in each file
- Outputs a sorted table of files by line count



author{Obed Adu-Gyamfi}